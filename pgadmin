the task that you provide earlier : 
- name: Set pgAdmin4 database name based on ENVI
  set_fact:
    pgadmin_db_name: >-
      {% if ENVI == 'oat' %}
        {{ vault_secret_response.json.data.data.pgadmin_db_name_oat }}
      {% elif ENVI == 'prd' %}
        {{ vault_secret_response.json.data.data.pgadmin_db_name_prd }}
      {% else %}
        "unknown_environment"
      {% endif %}
if retrive pgadmin_db_name = pgadmin_db_name_oat if ENVI = OAT and pgadmin_db_name = pgadmin_db_name_prd if ENVI = prd, the idea was to to retrive the content of pgadmin_db_name_oat and pgadmin_db_name_prd stored on vault_secret_response
help fix this issue and debug it 



####solution
