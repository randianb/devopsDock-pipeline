i go this output latest_os_image.py p is not good: 


knowing that 
def REGIONS = "paris,north" 
"regions=$REGIONS",

for (def region in regions) {
        container("python"){
          echo """
          +++++++++++++++++++++++ [${region}] +++++++++++++++++++++++
          """
          LATEST_OS_IMAGE = sh(script: """
            python latest_os_image.py $region"""
