i got this error "The error appears to be in '/home/jenkins/agent/workspace/XSF/APPS/PGADMIN4/CD_DEPLOY/PGADMIN4_DEV/pipeline/app/v2/ansible/roles/pgadmin4/tasks/pgadmin4.yml': line 201, column 3, but may
be elsewhere in the file depending on the exact syntax problem.

The offending line appears to be:


- name: Execute setup-web script
  ^ here"
here 's my ansible tasks: 
- name: Configure pgAdmin to use external PostgreSQL
  become: true
  template:
    src: config_local.py.j2
    dest: /etc/pgadmin4/config_local.py
    owner: root
    group: root
    mode: '0644'
  notify: Restart Apache HTTPD

- name: Execute setup-web script
  become: true
  shell: |
    /usr/pgadmin4/bin/setup-web.sh
  args:
    creates: /var/lib/pgadmin/pgadmin4.db
  environment:
    PGADMIN_SETUP_EMAIL: "{{ pgadmin_setup_email }}"
    PGADMIN_SETUP_PASSWORD: "{{ pgadmin_setup_password }}"
  ignore_errors: true
  force: yes
