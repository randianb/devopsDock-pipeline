do the same to this script : 
#!/bin/bash

# SMTP Configuration
SMTP_HOST="smtp-goss.int.world.socgen"
SMTP_PORT=25
MAIL_FROM="devops.monitoring@socgen.com"
MAIL_TO="anouar.harrou@socgen.com"

# Fetch server details
latest_paris="$1"
latest_north="$2"
account="$3"

# Function to send an email notification
send_email() {
    local subject="🚨 Image Mismatch Alert - Region Discrepancy Detected"
    local message="<html>
    <body style='font-family:Arial, sans-serif;'>
        <h2 style='color:#d9534f;'>🚨 Alert: Image Mismatch Detected</h2>
        <p style='font-size:14px;'>An inconsistency has been detected in the latest images deployed in different regions.</p>
        <hr>
        <h3 style='color:#0275d8;'>🔹 Account: <span style='color:#07c3ed;'>${account}</span></h3>
        <p style='font-size:16px;'>⚠️ The latest image versions are not aligned:</p>
        <ul>
            <li>📍 <b>Paris:</b> <span style='color:#07ed5f;'>${latest_paris}</span></li>
            <li>📍 <b>North:</b> <span style='color:#07ed5f;'>${latest_north}</span></li>
        </ul>
        <p>🔍 Please investigate the discrepancy and take necessary actions.</p>
        <hr>
        <p style='color:#5e5e5e; font-size:12px;'>📧 This is an automated notification from the DevOps monitoring system.</p>
    </body></html>"
    
    local temp_file=$(mktemp)

    echo -e "Subject: ${subject}\nContent-Type: text/html; charset=UTF-8\n\n${message}" > "${temp_file}"

    curl -s --url "smtp://${SMTP_HOST}:${SMTP_PORT}" --mail-from "${MAIL_FROM}" --mail-rcpt "${MAIL_TO}" --upload-file "${temp_file}"

    rm -f "${temp_file}"
}

# Execute the email function
send_email
