     def lines = result.split("\n") // Get all lines of output
        OS_FACTORY_IMAGE = lines[-1]   // Last line should be the value of OS_FACTORY_IMAGE
    } else {
        OS_FACTORY_IMAGE = "Nothing to compare."
    }